import { MetadataSelector } from "@/components/MetadataSelector";

/**
 * Index Page - Main entry point for the Customizable Front-End Engine
 * Showcases the dynamic rendering capabilities and metadata configuration
 */
const Index = () => {
  return <MetadataSelector />;
};

export default Index;
